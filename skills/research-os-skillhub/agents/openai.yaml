interface:
  display_name: "Research OS SkillHub"
  short_description: "Route local data into buy-side reports"
  brand_color: "#0F766E"
  default_prompt: "Use $research-os-skillhub to route local Research OS data into a Chinese buy-side report."

policy:
  allow_implicit_invocation: true
